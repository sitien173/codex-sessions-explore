# Optional: override sessions directory without editing docker-compose.yml
# Copy this file to .env and adjust as needed.

# Path to your Codex sessions directory on the host
SESSIONS_DIR=/data/share/.codex/sessions

# Port to expose the web server on
PORT=3000
